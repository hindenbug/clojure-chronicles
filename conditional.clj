;; Conditionals

;; if
;; logical true is anything other than nil or false
;; if a expressions is logically false no else is provided then it returns 'nil'


(if "hi" \t)


;; refinements built on top of if
;; when - best used when nil should be returned if a condition is false
;; cond - similar to else if (Ruby)
;; if-let and when-let